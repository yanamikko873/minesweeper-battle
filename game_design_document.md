# マインスイーパー対戦ゲーム 設計書・仕様書

**バージョン**: 2.0  
**作成日**: 2026-05-20  
**開発者**: Otake  
**開発環境**: VSCode + Claude Code

---

## 目次

1. [ゲーム概要](#1-ゲーム概要)
2. [技術スタック](#2-技術スタック)
3. [システムアーキテクチャ](#3-システムアーキテクチャ)
4. [ゲームルール仕様](#4-ゲームルール仕様)
5. [マッチング機能仕様](#5-マッチング機能仕様)
6. [おじゃまシステム仕様](#6-おじゃまシステム仕様)
7. [画面設計](#7-画面設計)
8. [API仕様](#8-api仕様)
9. [Socket.ioイベント仕様](#9-socketioイベント仕様)
10. [データ設計](#10-データ設計)
11. [AWSインフラ設計](#11-awsインフラ設計)
12. [開発ロードマップ](#12-開発ロードマップ)
13. [フォルダ構成](#13-フォルダ構成)

---

## 1. ゲーム概要

### コンセプト
マインスイーパーをベースに、ぷよぷよテトリス風の**おじゃまシステム**と**マルチプレイ**を組み合わせた対戦ゲーム。
毎日配信される共通のボードを4人で同時に解き合い、早く進むほど相手の盤面を邪魔できる。
ゴッドフィールドのようにブラウザで即プレイ可能・PC/スマホ両対応。

### ターゲット
- マインスイーパー好きなカジュアルゲーマー
- 毎日のパズルを習慣にしたいユーザー
- 友人と気軽に対戦したいユーザー

### 主要機能

| 機能 | 説明 |
|------|------|
| 毎日パズル | 毎日0:00に全プレイヤー共通のボードが配信される |
| ソロプレイ | 一人でタイムアタック |
| 合言葉マッチング | 合言葉を共有して最大4人で対戦 |
| ランダムマッチング | 見知らぬプレイヤーとマッチング |
| おじゃまシステム | 早く進むと相手のボードに妨害を送る |

---

## 2. 技術スタック

### フロントエンド

| 技術 | 用途 | バージョン |
|------|------|-----------|
| HTML5 | マークアップ | - |
| CSS3 | スタイリング・レスポンシブ | - |
| Phaser.js | ゲームエンジン・ボード描画 | 3.x |
| Socket.io-client | リアルタイム通信 | 4.x |

### バックエンド

| 技術 | 用途 | バージョン |
|------|------|-----------|
| Node.js | サーバーランタイム | 20.x LTS |
| Express | Webフレームワーク | 4.x |
| Socket.io | WebSocketサーバー | 4.x |

### インフラ（AWS）

| サービス | 用途 |
|---------|------|
| S3 | フロントエンド静的ファイル配信 |
| CloudFront | CDN・HTTPS対応 |
| EC2 (t3.micro) | Node.jsゲームサーバー |
| DynamoDB | 毎日パズルデータ保存 |

---

## 3. システムアーキテクチャ

```
[ブラウザ - PC/スマホ]
    HTML + CSS + Phaser.js + Socket.io-client
           |
           | HTTPS（静的ファイル）
           ↓
    [CloudFront + S3]
    フロントエンド配信

           |
           | WebSocket（Socket.io）
           ↓
    [EC2 - Node.js サーバー]
    Express + Socket.io
    ├── ルーム管理（メモリ）
    ├── 毎日ボード生成ロジック（地雷配置シード）
    └── おじゃまシステムロジック
           |
           ↓
    [DynamoDB]
    └── daily_boards テーブル（毎日の地雷配置）
```

---

## 4. ゲームルール仕様

### 4.1 ボード仕様

| 難易度 | 列×行 | 地雷数 |
|--------|-------|--------|
| 初級 | 9×9 | 10個 |
| 中級 | 16×16 | 40個 |
| 上級 | 30×16 | 99個 |

> 毎日パズルはデフォルト中級（16×16・40個）

### 4.2 基本ルール（マインスイーパー準拠）

1. セルを左クリック（タップ）で開く
2. 数字は周囲8マスの地雷数を示す
3. 地雷を踏んだらゲームオーバー（その場で脱落）
4. 全ての安全セルを開けたらクリア
5. 右クリック（長押し）で旗を立てる

### 4.3 毎日パズル仕様

```
配信時刻  : 毎日 00:00（JST）
内容      : その日共通のシード値から生成した地雷配置
目的      : 全員同じボードを解くことでフェアな対戦を実現
難易度    : 中級固定（16×16・40地雷）
初手保証  : 最初のクリックは必ず安全セル（標準ルール）
```

### 4.4 勝敗条件

```
勝利 : 相手より早く全安全セルを開けた人が勝ち
脱落 : 地雷を踏んだプレイヤーはその場でゲームオーバー
順位 : クリア時刻順（クリアできなかった人は地雷を踏んだ順）
```

### 4.5 操作仕様

| 操作 | PC | スマホ |
|------|----|--------|
| セルを開く | 左クリック | タップ |
| 旗を立てる/外す | 右クリック | 長押し |
| 周囲一括開放 | 数字セルをダブルクリック | 数字セルをダブルタップ |

---

## 5. マッチング機能仕様

### 5.1 合言葉マッチング

```
フロー:
1. プレイヤーAが合言葉を入力して「部屋を作る」
2. プレイヤーB〜Dが同じ合言葉を入力して入室
3. 部屋主が「ゲーム開始」ボタンを押す
4. カウントダウン（3秒）後ゲーム開始

仕様:
- 合言葉  : 英数字4〜8文字（大文字小文字区別なし）
- 最大人数: 4人
- 最小人数: 2人（部屋主は1人でも開始可能）
- 有効期限: 最後の操作から30分
```

### 5.2 ランダムマッチング

```
フロー:
1. 「ランダム対戦」ボタンを押す
2. 待機中の部屋に自動入室（なければ新規作成）
3. 2〜4人揃い次第カウントダウン開始（30秒）
4. 30秒後またはMAX4人揃い次第ゲーム開始

仕様:
- 最大待機時間: 60秒
- 60秒経過で2人以上いればゲーム開始
- 1人のみの場合は待機継続
```

### 5.3 部屋の状態管理

```javascript
Room {
  roomId    : string,
  roomCode  : string | null,   // 合言葉（ランダムはnull）
  players   : Player[],        // 最大4人
  status    : 'waiting' | 'playing' | 'finished',
  boardSeed : number,          // その日の地雷配置シード値
  createdAt : Date,
  isRandom  : boolean
}
```

---

## 6. おじゃまシステム仕様

### 6.1 おじゃまの発生条件

相手より先にセルを開いていくと、相手の盤面に妨害が送られる。

| トリガー | 送るおじゃま |
|---------|------------|
| 安全セルを5個開けるごと | 相手の開いていないセル1個を**偽フラグ**でマーク |
| 安全セルを15個開けるごと | 相手の開いているセル3個を**再び閉じる**（霧がかかる） |
| 相手が地雷を踏んだとき | 自分のボードの霧・偽フラグを1つ解除（ボーナス） |

### 6.2 おじゃまの種類

| 種類 | 見た目 | 効果 |
|------|--------|------|
| **偽フラグ** | 赤い旗（本物と区別つかない） | 誤った旗を立てて惑わせる |
| **霧（再封鎖）** | 開いていたセルが閉じる | 一度開けたセルが見えなくなる |
| **ノイズ** | 数字が一時的に±1ズレる | 5秒間だけ数字が嘘をつく |

### 6.3 おじゃまの送信ターゲット

```
4人対戦: ランダムな1人（自分以外）
3人対戦: ランダムな1人（自分以外）
2人対戦: 相手のみ
```

---

## 7. 画面設計

### 7.1 画面一覧

```
/ (トップページ)
  ├── 今日のパズル情報（難易度・日付）
  ├── ソロプレイ開始ボタン
  ├── 合言葉で対戦ボタン
  └── ランダム対戦ボタン

/solo (ソロプレイ画面)
  ├── マインスイーパーボード
  ├── タイマー
  ├── 残り地雷数カウンター
  └── リスタートボタン

/room/:roomCode (待機ルーム画面)
  ├── 参加者一覧（アイコン・名前）
  ├── 合言葉表示
  └── ゲーム開始ボタン（部屋主のみ）

/game/:roomId (ゲーム画面)
  ├── 自分のボード（大・メイン）
  ├── 相手のボード×3（小・リアルタイム表示）
  ├── タイマー
  ├── 残り地雷数
  └── おじゃま受信エフェクト
```

### 7.2 ゲーム画面レイアウト（PC）

```
┌──────────────────────────────────────────────┐
│  [相手1ミニボード]  [相手2ミニボード]  [相手3ミニボード] │
│                                               │
│            [自分のメインボード]                │
│       [残り地雷数🚩]  [タイマー⏱]             │
└──────────────────────────────────────────────┘
```

### 7.3 ゲーム画面レイアウト（スマホ）

```
┌──────────────────┐
│ [相手1] [相手2] [相手3] │
├──────────────────┤
│   [自分のボード]   │
│  [🚩残り地雷数]   │
│    [⏱タイマー]   │
└──────────────────┘
```

---

## 8. API仕様

### 毎日ボード取得

```
GET /api/daily-board
Response:
{
  "date"   : "2026-05-20",
  "seed"   : 98765,
  "rows"   : 16,
  "cols"   : 16,
  "mines"  : 40
}
```

### 部屋作成

```
POST /api/rooms
Body    : { "type": "private" | "random" }
Response:
{
  "roomId"  : "abc123",
  "roomCode": "MINE",   // privateの場合のみ
  "status"  : "waiting"
}
```

### 部屋入室確認

```
GET /api/rooms/:roomCode
Response:
{
  "roomId"     : "abc123",
  "playerCount": 2,
  "maxPlayers" : 4,
  "status"     : "waiting"
}
```

---

## 9. Socket.ioイベント仕様

### 9.1 クライアント → サーバー

| イベント名 | データ | 説明 |
|-----------|--------|------|
| `join_room` | `{ roomId, playerName }` | 部屋に入室 |
| `player_ready` | `{ roomId }` | 準備完了 |
| `start_game` | `{ roomId }` | ゲーム開始（部屋主のみ） |
| `cell_opened` | `{ roomId, count }` | 安全セルを開けた累計数 |
| `flag_placed` | `{ roomId }` | 旗を立てた |
| `game_clear` | `{ roomId, time }` | クリア完了・タイム送信 |
| `game_over` | `{ roomId }` | 地雷を踏んでゲームオーバー |

### 9.2 サーバー → クライアント

| イベント名 | データ | 説明 |
|-----------|--------|------|
| `room_updated` | `{ players, status }` | 部屋情報更新 |
| `game_start` | `{ seed, rows, cols, mines }` | ゲーム開始・ボードデータ |
| `receive_fake_flag` | `{ x, y }` | 偽フラグおじゃま受信 |
| `receive_fog` | `{ cells[] }` | 霧おじゃま受信 |
| `receive_noise` | `{ duration }` | ノイズおじゃま受信 |
| `player_board` | `{ playerId, openedCount, status }` | 他プレイヤーの進捗 |
| `player_eliminated` | `{ playerId }` | プレイヤー地雷踏み脱落 |
| `game_end` | `{ ranking[] }` | ゲーム終了・順位 |

---

## 10. データ設計

### 10.1 DynamoDB テーブル: daily_boards

```
PK         : date (String)  例: "2026-05-20"
Attributes :
  seed     : Number   // 乱数シード値（地雷配置に使用）
  rows     : Number   // 行数
  cols     : Number   // 列数
  mines    : Number   // 地雷数
  createdAt: String   // ISO8601
```

### 10.2 サーバーメモリ（ルーム管理）

```javascript
const rooms = new Map(); // roomId → Room

Room {
  roomId    : string,
  roomCode  : string | null,
  players   : Map<socketId, Player>,
  status    : 'waiting' | 'playing' | 'finished',
  boardData : object,
  createdAt : Date
}

Player {
  socketId      : string,
  playerName    : string,
  isReady       : boolean,
  isEliminated  : boolean,
  openedCount   : number,   // 開いた安全セル数
  clearTime     : number | null  // クリアタイム（ms）
}
```

---

## 11. AWSインフラ設計

### 11.1 構成図

```
インターネット
      ↓
[CloudFront]
  HTTPS終端・キャッシュ
      ↓              ↓
   [S3]           [EC2 t3.micro]
   静的ファイル     Node.js + Socket.io
   (HTML/CSS/JS)   ゲームサーバー
                       ↓
                   [DynamoDB]
                   毎日ボードデータ
```

### 11.2 EC2設定

```
インスタンスタイプ : t3.micro（無料枠）
OS               : Amazon Linux 2023
開放ポート        :
  - 22  (SSH)
  - 80  (HTTP)
  - 443 (HTTPS)
  - 3000 (Socket.io)
```

### 11.3 コスト見積もり

| サービス | 月額目安 |
|---------|---------|
| EC2 t3.micro | 無料枠（1年間）→ 約$10/月 |
| S3 | ほぼ無料（< $1） |
| CloudFront | ほぼ無料（< $1） |
| DynamoDB | 無料枠内 |
| **合計** | **初年度ほぼ無料** |

---

## 12. 開発ロードマップ

### Phase 1: ソロ版（1〜2週間）
- [ ] フォルダ構成作成・環境構築
- [ ] Phaser.jsでマインスイーパーボード表示
- [ ] セルのクリック・開放ロジック
- [ ] 数字の自動計算・表示
- [ ] 旗を立てる機能
- [ ] 地雷踏み・ゲームオーバー判定
- [ ] 全開放・クリア判定
- [ ] タイマー・残り地雷数表示
- [ ] スマホタッチ操作対応

### Phase 2: サーバー構築（1週間）
- [ ] Node.js + Express セットアップ
- [ ] Socket.io 基本通信
- [ ] 毎日ボードAPI（DynamoDB連携）
- [ ] シード値から地雷配置を生成するロジック

### Phase 3: マルチプレイ（1〜2週間）
- [ ] 合言葉ルーム機能
- [ ] ランダムマッチング
- [ ] 相手ボードの進捗リアルタイム表示
- [ ] プレイヤー脱落・クリア処理
- [ ] ゲーム終了・順位表示

### Phase 4: おじゃまシステム（1週間）
- [ ] 偽フラグ送信・表示
- [ ] 霧（再封鎖）送信・表示
- [ ] ノイズ（数字ズレ）送信・表示
- [ ] おじゃま受信エフェクト

### Phase 5: AWSデプロイ（3日〜1週間）
- [ ] S3 + CloudFrontにフロントをアップロード
- [ ] EC2にNode.jsサーバーデプロイ
- [ ] 独自ドメイン設定（任意）

---

## 13. フォルダ構成

```
minesweeper-battle/
├── frontend/
│   ├── index.html          // トップページ
│   ├── game.html           // ゲーム画面
│   ├── css/
│   │   ├── main.css        // 共通スタイル
│   │   └── game.css        // ゲーム画面スタイル
│   ├── js/
│   │   ├── main.js         // トップページロジック
│   │   ├── game.js         // ゲームメインロジック
│   │   ├── board.js        // ボード生成・管理
│   │   ├── cell.js         // セル操作ロジック
│   │   ├── ojama.js        // おじゃまシステム
│   │   ├── socket.js       // Socket.io通信
│   │   └── ui.js           // UI更新処理
│   └── assets/
│       ├── images/         // 旗・地雷・数字画像
│       └── sounds/         // 効果音
│
├── backend/
│   ├── server.js           // エントリーポイント
│   ├── socket/
│   │   ├── index.js        // Socket.ioイベント管理
│   │   ├── roomManager.js  // ルーム管理
│   │   └── ojamaLogic.js   // おじゃま送信ロジック
│   ├── routes/
│   │   ├── board.js        // 毎日ボードAPIルート
│   │   └── room.js         // 部屋APIルート
│   ├── services/
│   │   ├── boardService.js // ボード生成サービス
│   │   └── dynamoService.js// DynamoDB操作
│   └── package.json
│
├── infrastructure/
│   ├── deploy.sh           // デプロイスクリプト
│   └── nginx.conf          // Nginx設定（EC2用）
│
└── README.md
```

---

## 付録: VSCode推奨拡張機能

| 拡張機能 | 用途 |
|---------|------|
| ESLint | JavaScript構文チェック |
| Prettier | コード整形 |
| Live Server | ローカル確認用サーバー |
| Thunder Client | API動作確認 |
| GitLens | Git管理 |

---

*このドキュメントはゲーム開発の進行に合わせて随時更新してください。*
